# chef-cyclo bluetoothd configuration (overrides Alpine's defaults file).
[General]
Name = chef-cyclo
# LE only: sensors are BLE and the board's BR/EDR audio path is not wired up.
ControllerMode = le

[Policy]
# Power the adapter on as soon as btattach registers it.
AutoEnable = true
